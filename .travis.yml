language: python

python:
- '3.6'

install:
- pip install -r requirements.txt
- pip install -r test_requirements.txt
- pip install tox-travis

before_script:
  tox -e flake8

script:
  tox

after_success:
- coveralls
